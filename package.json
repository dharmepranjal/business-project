{
    "name": "signalrank-project",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
        "frontend"
    ],
    "scripts": {
        "dev": "npm run dev -w frontend",
        "build": "npm run build -w frontend",
        "start": "npm run start -w frontend",
        "install:frontend": "npm install -w frontend",
        "backend": "cd backend && python3 -m uvicorn app.main:app --reload"
    }
}